[project]
name = "librasnet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]

[tool.ruff]
exclude = [
    ".venv",
    ".git",
    ".github",
    "__pycache__"
]

[tool.ruff.lint]
select = ["E", "W", "F", "I"]
