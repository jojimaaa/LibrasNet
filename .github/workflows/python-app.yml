# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.12
        uses: actions/setup-python@v3
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          uv sync
      - name: Lint with ruff
        run: |
          # 1. Stop the build if there are Python syntax errors or undefined names
          uv run ruff check . --select=E9,F63,F7,F82
          # 2. Exit-zero treats all errors as warnings. (Checks complexity and line length)
          uv run ruff check . --exit-zero --config "lint.mccabe.max-complexity=10" --line-length=127
      - name: Test with pytest
        run: |
          uv run pytest -q
